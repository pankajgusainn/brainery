import React from 'react';

interface ParticleFieldProps {
  isActive: boolean;
}

export function ParticleField({ isActive }: ParticleFieldProps) {
  // Component now only serves as a placeholder for future dynamic particles
  // Static particles have been removed
  return null;
}